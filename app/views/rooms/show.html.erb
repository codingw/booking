<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @room.name %>
</p>

<p>
  <strong>Capacity:</strong>
  <%= @room.capacity %>
</p>

<%= link_to 'Edit', edit_room_path(@room), class: 'btn-success' %> |
<%= link_to 'Back', rooms_path, class: 'btn btn-warning' %>
